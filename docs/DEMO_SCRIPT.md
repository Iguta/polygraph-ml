# Demo Script — PolygraphML

**Maximum:** 3:00 · **Primary proof path:** 1:30 · **Status:** local fixture path verified; final live and fallback recordings open

## 0:00–0:12 — The claim

> “A model score is a claim. PolygraphML audits the model, data, notebook, and deployment scenario together—and shows the score the model actually earned.”

Open the flagship card. It identifies the licensed full UCI Bank Marketing bundle: 45,211 rows, a `.skops` model, executed notebook, derivation record, and pinned hashes.

## 0:12–0:27 — The missing context

Confirm the evidence map and show the prefilled prediction contract: the score must exist **before the current marketing call begins**.

> “The scenario changes the answer. A feature can be legitimate after a call and impossible before it.”

Start the audit. Briefly point to the durable audit ID, source/build provenance, and event transport. Refresh once only in the fallback recording; the restored trace proves resumability without consuming live-demo time.

## 0:27–0:55 — GPT-5.6 asks the decisive question

The Decision Trace shows the reported claim, exact reproduction, and the agent's falsifiable `post_outcome` hypothesis about `duration`.

The question is:

> “Is `duration` available at the declared decision moment, or only afterward?”

Choose **Only after the event**.

> “GPT-5.6 supplies the semantic investigation. It cannot write a metric or confirm a finding. Typed deterministic probes test association, availability, contamination, preprocessing, and the smallest correction.”

Do not call the Decision Trace chain-of-thought. It is a sanitized application-owned record of assumptions, hypotheses, tools, evidence, corrections, and falsifiers.

## 0:55–1:15 — The proof

Hold on the measured comparison:

```text
Reported    0.871301
Reproduced  0.871301  (0.8713005400607956 unrounded)
Corrected   0.733085  (0.7330845871361364 unrounded)
```

> “The notebook's score reproduces. But current-call duration does not exist at the pre-call decision. Removing that confirmed unavailable feature reduces ROC AUC by 13.8 points. Association alone did not prove leakage; scenario evidence plus deterministic impact did.”

Expand **What would change this?** and show the source/evidence references.

## 1:15–1:30 — Action, not accusation

Generate the executive brief, then build the deterministic repair. Show its SHA-256 and download action.

> “PolygraphML leaves receipts: an executive decision, a technical protocol, and a hash-addressed patch bundle. The hard negative is highly predictive but clears because it is available before the decision; two clean controls have zero false confirmations.”

End card:

> “The model reasons; code computes; evidence decides. PolygraphML—the QA layer for machine learning.”

## Optional 30-second technical appendix

Only if the format allows extra time:

- show `LIVE · gpt-5.6-sol`, agent harness `audit-v2`, bounded compute `compute-v1`, and matching frontend/backend release SHA;
- state that SQS preserves work across disconnects, DynamoDB stores ordered events, and S3 stores artifacts/repairs;
- show the seven-case fixture result as `6/6` expected pairs with a 95% Wilson interval `[0.609657, 1.0]`, not as unsupported broad proof;
- identify the 14-row UCI COVID case as a non-clinical clean adapter/provenance test.

## Recording rules

- Record live only after `make eval-live` passes exactly three predeclared cases and the deployed model/build provenance agrees.
- Keep a complete fixture recording as contingency; its badge must say `FIXTURE · no live model call`.
- Use the checked-in `benchmark-results/latest.json` values and the deployed `/version` response from the final release SHA.
- Record against `https://polygraphml.davidiguta.com`; verify the backend uses the same SHA before filming.
- Hide developer tools, secrets, presigned URLs, tokens, account identifiers, and cloud consoles.
- Keep the comparison card legible at 1080p and verify the file duration before upload.
- Make no production-performance, universal-safety, clinical, or causal claim.

## One-slide summary

**Reported → Reproduced → Corrected**, with:

> “A model score is a claim. PolygraphML audits the evidence.”
